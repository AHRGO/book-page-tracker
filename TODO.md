# Things to add in the future
## Features
- Add the option for the user, when registering a new tracking project, register the type of what it will be tracking aka book, course, game, etc

- An option to send the internal json structure to the user email (without saving it anywhere)
    - or enabling login with google and saving the progress in firebase? So, need to create firebase for this app.

- on the card which shows the % of conclusion, add the action of user tap the card and then a modal is prompted, saying "where are you now?" and the user simply enters the number. After all, the main action the user will be performing on this screen is to updating it's progress
    - The card will have the pencil icon for deep edition tho
    - Add the option for the user to input it's impressions within a new entry of the tracker

- Add the option for the user to enter the author(s) of the content.

- Add the option for the user to select the type of "thing" used to control the progress aka pages, chapters, days, anything.

- Add internationalization, English, Brazilian, Spanish

# Overall Project
- Change the project name to "Progress Tracker", as it evolved to be a general progress tracker instead of only books
- Remove `ios` and `macos` folder, since I ain't planning to release this into app store.